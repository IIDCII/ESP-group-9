<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ESPG9app" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">#E1E1E1</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="backgroundFeedView">
        <item name="android:background">#F7F7F7</item>
    </style>
    <style name="StudentMainCardView">
        <item name="cardBackgroundColor">@color/white</item>
    </style>
    <style name="StudentMainCardText">
        <item name="android:textColor">#000000</item>
    </style>
    <style name="SusRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/sus_rating</item>
            <item name="android:minHeight">23dip</item>
            <item name="android:maxHeight">100dip</item>
    </style>
    <style name="AppTheme">
        <item name="android:windowActionBarOverlay">true</item>
    </style>
</resources>